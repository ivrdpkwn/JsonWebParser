5️⃣ utils
职责：读文件,工具函数


一、FileUtil 层的目标

它主要负责 文件读取/写入（比如读取前端 HTML、CSS、JS 文件），让后面的 Controller 层可以直接用。

功能列表（最小可行版）

1.读取文本文件

输入：文件路径

输出：字符串内容

遇到错误返回异常或空字符串

2.写文本文件（可选）

输入：文件路径 + 内容

输出：是否成功

3.（可选）检查文件是否存在

输入：路径

输出：bool

对于我们的 JSON 解析器项目，最重要的是第 1 个功能，其他可以先留空。